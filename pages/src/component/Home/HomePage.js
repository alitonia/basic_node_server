import './HomaPage.css';

export const HomePage = (props) => {
  return (
      <div className={'welcome_container'}>
        <h1 className={'welcome_text'}>Welcome to Bomb server</h1>
      </div>
  );
};